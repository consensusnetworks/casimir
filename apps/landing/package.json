{
  "name": "@casimir/landing",
  "private": true,
  "source": "src/index.html",
  "scripts": {
    "dev": "parcel 'src/html/*.html' --no-cache --cache-dir ./.parcel-cache",
    "build": "parcel build 'src/html/*.html' --no-cache --cache-dir ./.parcel-cache",
    "clean": "rm -rf dist .parcel-cache"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.9.3",
    "gray-matter": "^4.0.3",
    "marked": "^9.0.0",
    "parcel": "^2.9.3"
  },
  "dependencies": {
    "motion": "^10.16.2",
    "snarkdown": "^2.0.0"
  }
}