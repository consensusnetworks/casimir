{
  "name": "@casimir/users",
  "version": "0.0.1",
  "main": "src/index.ts",
  "scripts": {
    "build": "node build.js",
    "clean": "ts-node --transpile-only scripts/clean.ts",
    "dev": "ts-node --transpile-only scripts/dev.ts",
    "watch": "ts-node-dev --watch ../../common/data/src --respawn --transpile-only scripts/dev.ts",
    "seed": "ts-node --transpile-only scripts/seed.ts --resource \"$npm_config_resource\"",
    "test": "echo \"Warning: no test specified\" && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "ethers": "^5.7.2",
    "express": "^4.18.1",
    "supertokens-node": "^13.1.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.38",
    "@types/pg": "^8.6.6",
    "chai": "^4.3.6",
    "esbuild": "^0.15.9",
    "minimist": "^1.2.8",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0"
  }
}
