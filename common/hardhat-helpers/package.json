{
    "name": "@casimir/hardhat-helpers",
    "private": true,
    "main": "src/index.ts",
    "scripts": {
        "build": "echo 'Warning: @casimir/hardhat-helpers build not specified. Disregard this warning and any listed errors above if @casimir/hardhat-helpers is not needed for the current project build.' && exit 0",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@nomiclabs/hardhat-ethers": "^2.0.6",
        "@typechain/ethers-v5": "^10.1.0",
        "@typechain/hardhat": "^6.1.2",
        "@types/node": "^17.0.38",
        "esbuild": "^0.15.9",
        "hardhat": "^2.12.2",
        "typechain": "^8.1.0"
    }
}
