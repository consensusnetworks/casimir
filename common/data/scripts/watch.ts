import { $ } from 'zx'
import { parseStdout } from '@casimir/zx-helpers'

/**
 * Watch @casimir/data schemas for changes and generate new sql files
 */
void async function () {
    console.log('Hello')
}