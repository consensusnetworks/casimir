/**
 * Use to pass constructor arguments to hardhat-verify
 * npm run verify --workspace @casimir/ethereum -- --constructor-args arguments.js --network goerli CONTRACT_ADDRESS
 */

// const { ETHEREUM_CONTRACTS } = require("@casimir/env");

module.exports = []